# Method 2 — Codex cloud

**Status: procedure written (automation-dispatch, Codex flavour);
experiment BLOCKED: no Codex/ChatGPT account available. No verdict is
recorded until one runs — a faked verdict would poison the comparison table.**

## What it is

The same push-only shape as method 1 on the OpenAI side: a hosted session
reached via the Codex surface in ChatGPT, handed a task, returning a PR.

## How it plugs in here

Identical to method 1 — same skill, same brief format, same standing block,
same gate on return. Only the hand-off target differs. Its value is as a
cross-vendor comparison point: same issue shape, different engine.

## Experiment log

### 2026-07-22 — blocked: no account

Procedure verified as far as writable; the dispatch skill treats Codex as a
target already. When an account exists: run the same experiment-1 shape as
method 1 (one small feature issue, end to end) and fill the same columns
(setup friction, wall time, cost, PR quality, verdict).
