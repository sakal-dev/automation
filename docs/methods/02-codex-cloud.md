# Method 2 — Codex cloud

**Status: not started.**

## What it is

The same shape as method 1 on the OpenAI side: a hosted session reached via
ChatGPT, handed a task, returning a PR. Push-only.

## How it plugs in here

The same `automation-dispatch` skill, Codex flavour — only the hand-off step
differs (dispatch outline step 3). Everything before (claim, brief) and after
(gate) is identical to method 1. Useful as a cross-vendor comparison point
for the lab notebook more than as a second production executor.

## Experiment log

*(empty — record setup, cost, and verdict per run here)*
