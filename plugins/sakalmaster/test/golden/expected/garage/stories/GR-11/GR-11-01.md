---
key: GR-11-01
title: Unified search — plate, phone, or membership
epic: GR-11
persona: operator
app: garage-flutter
tags: [P0]
out_of_scope: []
source: sakal-dev/garage-flutter:docs/specs/GR-11-car-care-bay-operations.md#gr-11-01--unified-search--plate-phone-or-membership@523e808
---

## Acceptance criteria

```yaml
- ac: GR-11-01-a
  marker: "[x]"
  text: "✅ Built — `SearchQuery.parse` infers plate/phone/membership with no mode toggle (`core/search/search_query.dart`, unit-tested); the UI hint shows the inferred kind (`search_view.dart`)."
  cite: []
- ac: GR-11-01-b
  text: "🔴 Not built — phone lookup is deliberately routed to `SearchOutcome.unsupported` (`search_controller.dart`). `ApiProvider.lookupCustomer` still exists, is **never called**, and still posts `{'phone': ...}` to `/api/pos/v1/customers` rather than the `?query=` endpoint. Both ends unfixed."
  cite: []
- ac: GR-11-01-c
  text: "🔴 Not built — membership-number lookup is also routed to `unsupported`; no endpoint exists."
  cite: []
- ac: GR-11-01-d
  marker: "[x]"
  text: "✅ Built — a single plate match navigates straight to the vehicle record instead of rendering a one-item list (`search_controller.dart` → `search`, `if (found.length == 1) openHistory(found.first)`); multi-match still renders the disambiguation list. *(GR-11-01a)*"
  cite: []
- ac: GR-11-01-e
  marker: "[x]"
  text: "✅ Built — an offline plate miss now resolves to `SearchOutcome.offline` and shows a distinct \"can't check while offline\" message rather than `notFound`; the rule is pure and unit-tested (`core/search/search_outcome.dart` → `plateLookupOutcome`, `test/core/search_outcome_test.dart`; wired in `search_controller.dart`, rendered by `_Offline` in `search_view.dart`). *(GR-11-01a)*"
  cite: []
- ac: GR-11-01-f
  text: "🟡 Partial — the home search bar now routes to unified search, but the **History screen keeps its own plate-only search** (`history_controller.dart`), so unified search is an *added* surface, not yet a *replacement*."
  cite: []
```
