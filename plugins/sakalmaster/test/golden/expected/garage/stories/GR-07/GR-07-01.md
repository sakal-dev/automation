---
key: GR-07-01
title: Customer sign-off
epic: GR-07
persona: operator
app: garage-flutter
tags: [P1]
out_of_scope: []
source: sakal-dev/garage-flutter:docs/specs/GR-07-customer-signoff-and-share-proof.md#gr-07-01--customer-sign-off@523e808
---

## Acceptance criteria

```yaml
- ac: GR-07-01-a
  marker: "[x]"
  text: "✅ Built — on-screen signature capture attached to the job. `SignoffView` (`lib/app/modules/signoff/signoff_view.dart`) captures with a `SignatureController` and, on save, stores the PNG via `MediaRepository.attachBytes(kind: PhotoKind.signature)` and stamps the path with `JobFlowController.setSignaturePath` (`job_flow_controller.dart:145`)."
  cite: []
- ac: GR-07-01-b
  marker: "[x]"
  text: "✅ Built — the job summary (`_WorkSummary`) and the before/after shots (`_BeforeAfterStrip`) sit above the pad so the customer reviews the work before signing (`signoff_view.dart`)."
  cite: []
- ac: GR-07-01-c
  marker: "[x]"
  text: "✅ Built — the signature is stored as a `JobPhotos` outbox row (`PhotoKind.signature`) via `MediaRepository.attachBytes` (`media_repository.dart:41`) — captured offline and uploaded by the media queue like any other photo."
  cite: []
- ac: GR-07-01-d
  marker: "[x]"
  text: "✅ Built — sign-off is optional: reached only from the submit screen (`SubmitController.goSignoff`, `submit_controller.dart:42`), never gates `submit()`, and the screen states `signoff.optional`."
  cite: []
```
