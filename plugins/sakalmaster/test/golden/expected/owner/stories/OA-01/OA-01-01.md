---
key: OA-01-01
title: App bootstrap and architecture skeleton
epic: OA-01
journey: OA-J1
persona: developer
app: owner-flutter
module: shell
tags: [P0]
out_of_scope: []
source: sakal-dev/sakalpos-owner:docs/specs/OA-01-platform-auth-shell.md#oa-01-01--app-bootstrap-and-architecture-skeleton@1e272bc
---

As a developer, I want the app scaffolded online-first to the shared
conventions, so that it's consistent with pos-flutter but doesn't drag in the
offline engine.

## Acceptance criteria

```yaml
- ac: OA-01-01-a
  text: "`lib/app/` structure per README §2: `core/`, `data/{models,providers,repositories,stores,services}`, `modules/*`, `middleware/`, `routes/`, `theme/`, `components/`"
  cite: []
- ac: OA-01-01-b
  text: "Dependencies: `get`, `dio` (or GetConnect), `firebase_messaging` + `firebase_core`, `flutter_secure_storage`, `local_auth`, `get_storage`, `intl`, a charts lib (`fl_chart`); **no Drift**"
  cite: []
- ac: OA-01-01-c
  text: "GetX routing with initial route resolved by auth state (logged-out → login; logged-in → dashboard)"
  cite:
    - kind: enforced
      path: lib/app/middleware/auth_middleware.dart
      symbol: AuthMiddleware
      sha: 1e272bc
      note: redirect guard enforcing auth-state routing
- ac: OA-01-01-d
  text: "Theme + EN/KM i18n reuse the pos-flutter approach (brand colours, Kantumruy Pro for Khmer)"
  cite:
    - kind: enforced
      path: lib/app/theme/app_theme.dart
      symbol: AppTheme
      sha: 1e272bc
    - kind: enforced
      path: lib/app/core/i18n/app_translations.dart
      symbol: AppTranslations
      sha: 1e272bc
- ac: OA-01-01-e
  text: "`core/` holds pure logic only (formatting, delta math) — no Flutter imports"
  cite: []
- ac: OA-01-01-f
  text: "A single `ApiProvider` base with token injection + standard error mapping (401/403/409/timeout)"
  cite:
    - kind: enforced
      path: lib/app/data/providers/api_provider.dart
      symbol: OwnerApiProvider
      sha: 1e272bc
```
